apiVersion: v1
kind: Service
metadata:
  name: backend-image-filter
  labels:
    service: backend-image-filter
spec:
  ports:
  - name: "8081"
    port: 8081
    targetPort: 8081
  selector:
    service: backend-image-filter